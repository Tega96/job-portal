import { memo } from 'react';

const ApplicationViewer = () => {
  return (
    <div className="ApplicationViewer">
      <h2>ApplicationViewer</h2>
    </div>
  );
};

export default memo(ApplicationViewer);